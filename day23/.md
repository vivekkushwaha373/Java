input from User

types of I/O

--> Console I/O
--> File I/O
--> Network I/O
--> Memory I/O

Let discuss System

System is a class

java doesn't need any import to use it

System in define in java.lang which is imported befault without u writing the code

System.out

out is static member of type printStream class

System.err

err is also static member of type printStream class

System.in

in is static member of type inputStream class


Streams meaning: flow of data 

1. inputstream: data flow into program
2. outputstream: data flow outside program

InputStream and OutputStream are abstract classes in Java
read()            write()

printStream extends Outputstream and give implementation of write method

data can be read from:
files
memory
console

All of them are "Stream of Bytes"

InputStream (Abs class) -->read()
1. FileInputStream
2. ByteArrayInputStream
3. BufferedInputStream // concrete class of System.in
4. DataInputStream

OutputStream (Abs class) ---> write()
1. FileOutputStream
2. ByteOutputstream
3. BufferedOutputStream 
4. PrintStream 

.read() only reads one byte

BufferedReader()
--> Read chunk of Character from OS Buffer
--> Store it in memory
--> Give them to program when required


              RAM
┌───────────────────────────────────────┐
│                                       │
│  OS page cache                        │
│  ┌─────────────────────────────┐     │
│  │ bytes from data.txt(bytes)  │     │
│  └─────────────────────────────┘     |
|   inputStream converys it into       |
|   stream of chars                     |             │
│                                       │
│  Java BufferedReader buffer           │
│  ┌─────────────────────────────┐     │
│  │ characters: H e l l o ...  │     │
│  └─────────────────────────────┘     │
│                                       │
└───────────────────────────────────────┘


reading input from java buffer is Really fast


KEYBORAD INPUT ---> os buffer (stores stream of bytes ) ---> read() reads charcater by character to its program which is slow

Buffer reader requires stream of chars hence InputStreamReader converts os buffer data to stream of chars not Buffer Reder reads it in chunk


Reader class(abstract has reader())
1. InputStreamReader
2. BufferedReader
3. FileReader


