Collection unterface has default methods called stream() and parallelstream()

hence every class like linkedlist or arraylist, hashset etc will have stream() and parallelstream overrides if exists any

What is stream in Java?

A stream is a tool of preossing a sequence of data through a chain of operation

// stream pipleline arhitecture

surce --> Intermediate  Operation --> Terminal operation


list.stream() // source piplline job do
     .filter(x->x>b)  // intermediate operation
     .map(x->x*2)    // intermediate operation
     .tolist()          // terminal 

     Note: Without termial operation steam will not work



Source:  --> collection
         --> arrays
         ---> stream.of()
         -->  Infinite stream    Stream.iterate(1,x->x+1)   and Stream.generate(Math::random) it takes supplier
         --> Stream.empty()

Intermediate aka Transformation
         ---> filter
         ---> map
         --> sorted
         --> distint
         --> limit
         --> skip

return type of intermediate operation are stream only


Terminal Operation

--> forEach
--> toLlist
--> collect
--> count()
--> findFirst()

it returns collection or consume printing something on the screen

Note:  NO Terminal Opration == No stream exeuted

READ LAZY evaluation: stream will process only after terminal operation: check for on internet ://HW

Once a stream is consumes it can not be consumed again

// Java also provides primitive stream

like intstream longstream and DoubleStream

BaseStream ----|---> Stream
               |--> IntStream
               |--> longStream
               ---> DoubleStream

.mapToDouble non primitive to primitive

.maptoInt


.boxed()  primitive to non primitive

            
// collector interface

which has collect method and in terminal operation we use collect() which expects collector object which is given by a utility class methods of collectors class

