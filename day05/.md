// Type casting in Java

1. implicit 
2. Excplicit

Rules 

for example
byte a =2;
int b=2; this is implicit casting : --> type widening

int a =3;
byte b = (byte)a; this is Explicit Casting : --> type narrowing

// type trucating

even if int and floats is a size of 4 byte yet u can't do

int a = 2.3f; coz int is not capable of holding floats it needs to 
truncate . somting part out of float to make it int

// boolean can be assigned to any type be it implict and explicit 

boolean a =false;

int a = b
or
int a= (boolean)b

// both are invalid


// type promotion


byte , short or char is promoted to int in an expression

float + other data type is like int is promoted to float

double with other data type like float or int promoted to double

