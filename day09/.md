functional Interface

1 interface with only one abstract method

interface Calsy{
    int add();
}


// H.W study callable and future as well


// static method can also have return type

// contructor can also be private public , protected, default

// Lambda expression only works on funtional Interface

if u don't use lambda expression you will need to first create a class override that funtion the u could have used it but with labda expression there is a shoirt to create object without creating class and give the implentation as well

// static memmer or properties of an interface can only be accessed by interface only
// all properties and methods in interface are public which can not modified: the acces modifier I meant
// if 2 interface with same abstract method is implented by class then class needs to define that astract methods only once
 