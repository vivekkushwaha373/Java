upcasting and downcasting in Java


static method should have their own type parameter but instance method type parameter will use class one

upcasting is implicit 

Integer x =10;
Object y=x;

downcasting is explicit

Object y =10;
Integer x = (Integer) y;

downcasting just prevent compile time error and hence generics come into the picture ,
runtime time throws classCastException

downcasting and upcasting works in parent child relation

// u can make a class and method generic isntead of specific

topics

bound generics or bound type // already studied
wildcards in generics to study 


## there are three types of wildcars in Java
- Upper bound wildcard
- Lower bound wildcard
- Unbounded wildcard
