The methods to prevent a thread from execution
1. Yield()
2. join()
3. sleep()

yield(): this method give hint to the T.S(Thread schedular) that the current is willing to yield() it's current use of the processor. it allow the other thread of equal priority to run before the current thread

join(): this method allow on thread to wait for the completion of another thread

sleep(): if we want to make current to sleep for some time we can use it


=================================================================

LifeCycle of thread 

1 new state 2 ready state  3 running state 5 dead state
                |                    |
                |____________________|
                   4  blocked/waiting state/ for IO

=======================================================================
 
in yield a process navigate from running state to ready state in case of yield
thread from a same process has same priority unless u explicitly chnage it using setPriority

There are a few OS that doesn't support yeild it can just request Schedular but schedular would not nessarily Obey

Thread

=====================================================================================

Inter Thread communication

1. wait()
2. notify()
3. notifyAll()

Wait(): this method i called on an object within a synchronized context/area to make the current wait until another thread invokes notify or notifyAll
when thread calls wait() it release the lock on the object and enter into the waiting state 

Notify(): this is called on an Object within synchronized context to wake up a waiting thread. When notify() is invoked it notifies on of the thread that are in waiting on the object to wake up. the choice of which thread to notify is not specifies and depends on the JVM.

NotifyAll(): this method is similar to notify(), but it wakes up all thread that are waiting on the same object 



Interview question that what is the difference of wait and join

Waiting thread never runs unless notified      Bit join thread runs when other thread completes it tasks

Waiting thread works in sycnchroniation        Join Thread doesn't work in synchronization context
context

Waiting thread releases a lock                 Join thread doesn't release any Lock

==================================================================================================


Java 8

1. functional interface 
2. Lambda expression
3. Predicate
4. Function
5. stream

=============================================================================================

An interface can contain:

Abstract methods
Default methods
Static methods
Constants (public static final fields)